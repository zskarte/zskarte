import { ReplaceAllAddressTokensPipe } from './replace-all-address-tokens.pipe';

describe('ReplaceAllAddressTokensPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceAllAddressTokensPipe();
    expect(pipe).toBeTruthy();
  });
});
