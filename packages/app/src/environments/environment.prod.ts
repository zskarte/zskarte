// noinspection JSUnusedGlobalSymbols

export const environment = {
  production: true,
  apiUrl: 'https://api.zskarte.ch',
  // new fastify/trpc backend, replaces `apiUrl` once strapi is gone
  apiUrlNext: 'https://api.zskarte.ch',
};
